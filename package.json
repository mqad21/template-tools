{
  "name": "fasih-form-studio-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm build:web && pnpm build:extension",
    "build:web": "pnpm --filter web build",
    "build:extension": "pnpm build:web && powershell -Command \"Remove-Item -Recurse -Force apps/extension/public/web -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path apps/extension/public/web -Force; Copy-Item -Recurse apps/web/dist/* apps/extension/public/web/\" && pnpm --filter extension build",
    "watch:extension": "pnpm --filter extension watch",
    "preview": "pnpm --filter web preview"
  }
}
